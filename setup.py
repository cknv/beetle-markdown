from setuptools import setup

setup(
    name='beetle_markdown',
    author='Esben Sonne',
    author_email='esbensonne@gmail.com',
    packages=[
        'beetle_markdown'
    ],
    install_requires=[
        'Markdown==2.4.1',
    ],
)
